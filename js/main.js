/**
 * Main Entry Point
 * Initializes all page modules once the DOM is ready.
 */
document.addEventListener('DOMContentLoaded', () => {
  initStickyHeader();
  initImageCarousel();
  initZoom();
  initMobileNav();
  initCarousel();
  initProcessTabs();
  initFaqAccordion();
  initCatalogueForm();
  initModals();
});
